URL = "http://127.0.0.1:8000/"

RESULTS_PATH = "./tests/results/"

ERROR_SLEEP_TIME = 1

PAYLOAD_CONFIGURATION = {
    "params": [
        {
            "tool": "masking",
            "fields": [
                    "nome"
            ]
        }
    ]
}
